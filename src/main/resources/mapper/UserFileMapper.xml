<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zsp.mapper.UserFileMapper">
    <select id="queryByUserId" resultType="userFile" >
    select * from user_file where user_id=#{userId} and parent_id=0
</select>
<select id="queryByParentId"   resultType="userFile"  >
    select * from user_file  where user_id=#{userId} and parent_id=#{parentId}
</select>
    <select id="queryByFileName"   resultType="userFile"   >
    select * from user_file  where user_id=#{userId} and file_name=#{fileName}
</select>
</mapper>